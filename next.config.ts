/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove experimental.appDir as it's now default in newer Next.js versions
  // Remove the postcss configuration from here
}

module.exports = nextConfig